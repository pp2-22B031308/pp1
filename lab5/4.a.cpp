#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
using namespace std;

int main(){
    string s;
    cin >> s;
    int k= 0, m= 0;

    for (int n = 0; n < s.size(); i++){
        if (str[i] >= 'a' && str[i]<= 'z')
        cnt++;
    }
    for (int i = 0; str[i] != '\0'; i ++){
        if (str[i] >= 'A' && str[i] <= 'Z')
        m = m+ 1;
    }
    cout << k << " "< m;
    return 0;
}