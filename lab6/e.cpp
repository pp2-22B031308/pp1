#include <iostream>
#include <iomanip>
#include <cmath>
using namespace std;
int h;

double hyp(int a, int b){

  cout << setprecision(4)<<(sqrt((a * a) + (b * b)));

  return h;
   
}

int main(){

     long long a, b;
  
     cin >> a >> b;

     hyp(a,b);

    return 0;
}